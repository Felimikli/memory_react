import Button from "react-bootstrap/Button";

function BlockExample() {
  return (
    <div className="sgb_cont">
      <Button className="startGameButton" size="lg">
        start game
      </Button>
    </div>
  );
}

export default BlockExample;
